
structure manifold
  head type

  link set, like set, have type
  link charts, like chart-collection, have type

  # Local Euclidean property
  assert local-euclidean
    head type, have type
    have set
    have charts

  # Compatibility of charts
  assert chart-compatibility
    head type, have type
    have charts
